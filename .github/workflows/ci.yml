name: CI Pipeline

on:
  push:
    branches: [ testing, main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # ==========================================
  # JOB 1: BACKEND TESTS + CODECOV
  # ==========================================
  test-backend:
    name: Test Backend (Unit Tests)
    runs-on: ubuntu-latest
    
    services:
      mongodb:
        image: mongo:5
        ports:
          - 27017:27017

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: server/package-lock.json

    - name: Create Backend Config
      run: |
        cd server
        cat > backend_config.json << EOF
        {
          "JWT_SECRET": "ci_testing_secret_123",
          "MONGO_URI": "mongodb://localhost:27017/testdb"
        }
        EOF

    - name: Install dependencies
      run: |
        cd server
        npm ci

    - name: Run Backend Unit Tests
      run: |
        cd server
        # Importante: --coverage genera el reporte que Codecov necesita
        npm test -- test/ --runInBand --silent --passWithNoTests --forceExit --detectOpenHandles --coverage

    # --- CORRECCIÓN: Codecov va AQUÍ, justo después de los tests ---
    - name: Upload Backend Coverage to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        flags: backend

  # ==========================================
  # JOB 2: FRONTEND TESTS + CODECOV
  # ==========================================
  test-frontend:
    name: Test Frontend (Unit Tests)  
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: client/package-lock.json

    - name: Install dependencies (client)
      run: |
        cd client
        npm ci

    - name: Run Frontend Tests
      env:
        CI: true
      run: |
        cd client
        # Vitest genera cobertura si está configurado en package.json o vite.config
        timeout 120s npx vitest run --coverage --reporter=verbose --pool=forks --no-cache --bail=1 --testTimeout=30000 --hookTimeout=30000

    # --- CORRECCIÓN: Codecov va AQUÍ también ---
    - name: Upload Frontend Coverage to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        flags: frontend
        
  # ==========================================
  # JOB 3: SONARCLOUD (CALIDAD)
  # ==========================================
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  Codecov:
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}